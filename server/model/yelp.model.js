var _ = require('underscore');

var yelpModel = module.exports

yelpModel.getActivities = function() {
	//do work for the function
	return new Promise(resolve, reject) {
		//make function call and then resolve it
	}
}